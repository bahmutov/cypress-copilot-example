Your goal is to create a new Cypress spec file

Ask for the feature name to be tested.

Use the application name TodoMVC and the feature name in the suite name inside the spec file

Use the related code from the folder "cypress" when answering the question

When importing symbols to the spec file, use the following import statement:

```javascript
import { TodoMVC } from '../../src/TodoMVC'
```
